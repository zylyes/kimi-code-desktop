### 改进

- **窗控区颜色运行时同步**：主窗口 Web UI 页实时采样右上角实际背景色并动态同步悬浮窗控配色（symbolColor 随亮度自适应），预览栏、改动条等任意顶栏状态下窗控与页面背景融为一体，彻底消除「补丁」感。
- **应用菜单重做（Kimi 官方风自绘面板）**：右上角合并为单个 ☰ 按钮（与窗控同排同风格），点击展开自绘下拉面板——圆角卡片浮层、分组标题、勾选态、快捷键提示、模型/多实例二级子面板、亮暗双主题；全窗口统一入口（Web UI 页注入，本地页统一挂载）；原生菜单仅保留作快捷键载体。
- **新建对话确认保留并鲁棒化**：实测官方按钮原地切换新空会话（无重载、无新进程），改为候选选择器数组逐个尝试。
- **全页面官方风美化**：十个原生页面逐页对齐 kimi-theme.css 令牌（色值清零硬编码、圆角/字号/间距统一、按钮归一、空态/加载态共享组件、暗色逐页核验）。

### 其他

- 截图脚本离屏连拍稳定性修复；新增单页截图/加载探针脚本。
- 无破坏性变更

📦 下载：见下方 `KimiCodeDesktop-Portable.exe`
